# dependencies (added at Tier 1)
node_modules/
.next/
dist/
build/
# vendor/ mirrors each package's own dist/ folder name so its internal
# relative imports keep resolving — un-ignore it, it's committed on purpose.
!vendor/**/dist/

# env / local
.env*
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/

# keep source font zips out of the repo (extracted into /fonts)
Typography/*.zip
